# VisionOps Sample Data

This folder contains small synthetic data used only for Demo Mode.

It does not include the WildNight dataset, private file paths, or YOLO model weights. The CSV files and SVG images are handcrafted examples that let a cloned repository demonstrate the dashboard, experiment comparison, failure gallery, and inference API shape.

